import { useEffect } from "react";
import api from "../services/api";

function Tickets() {
  {
    /** 
    async function getTickets(){
        let tickets = await api.get('/tickets')
        console.log(tickets)
    }

    useEffect(() => {
        getTickets()
    }, [])
    */
  }

  return <div></div>;
}

export default Tickets;
